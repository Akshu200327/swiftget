{
  "name": "swiftget",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node backend/server.js",
    "dev": "cd backend && npm run dev",
    "server": "cd backend && npm run dev",
    "install:all": "npm install",
    "build": "echo \"No build step needed; Express serves frontend files directly\""
  },
  "dependencies": {
    "compression": "^1.8.1",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "helmet": "^8.2.0"
  }
}
