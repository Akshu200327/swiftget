{
  "name": "swiftget-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "echo \"Frontend is served by the backend Express server. Run npm start from the project root.\"",
    "build": "echo \"No build step needed for vanilla frontend\""
  }
}
